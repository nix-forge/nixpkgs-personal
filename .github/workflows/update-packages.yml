name: Update packages
on:
  schedule:
    - cron: "23 4 * * 1"
  workflow_dispatch:
concurrency:
  group: package-updates
  cancel-in-progress: false
permissions: {}
jobs:
  update:
    # Updaters inspect macOS release archives with native extraction tools.
    # Upstream executables are checked only by the read-only package build CI.
    runs-on: macos-26
    timeout-minutes: 90
    permissions:
      actions: write # Dispatch trusted queue validation.
      contents: write # Update trusted repository automation.
      pull-requests: write # Update pull request metadata.
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up Nix
        uses: nix-forge/ci/actions/setup-nix@a75d21dababfd55145e2df1db81f3d2d865bec59 # v1.0.0
        with:
          cache: 'false'
          cache-scope: update-packages-update
      - name: Configure GitHub CLI authentication
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh auth setup-git
      - name: Update pinned sources
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: nix develop --command python scripts/update-packages.py --all --keep-going
      - name: Create or update the auto-merge pull request
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet; then
            echo "All package pins are current."
            exit 0
          fi

          branch="automation/package-updates"
          git fetch origin "$branch" || true
          git switch --force-create "$branch"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add pkgs scripts
          git commit -m "maint: update package sources"
          git push --force-with-lease --set-upstream origin "$branch"

          pr_url=$(gh pr list --repo "$REPOSITORY" --head "$branch" --state open --json url --jq '.[0].url')
          if [[ -z "$pr_url" ]]; then
            pr_url=$(gh pr create \
              --repo "$REPOSITORY" \
              --base main \
              --head "$branch" \
              --title "maint: update package sources" \
              --body "Automated package source updates.")
          fi

          # Pull requests created with GITHUB_TOKEN require workflow approval.
          # Explicit workflow_dispatch events are exempt and publish the same
          # required checks against the generated update commit.
          gh workflow run ci.yml --repo "$REPOSITORY" --ref "$branch"
          gh workflow run codeql.yml --repo "$REPOSITORY" --ref "$branch"
          gh workflow run dependency-review.yml --repo "$REPOSITORY" --ref "$branch" \
            -f base_ref=main \
            -f head_ref="$branch"

          # GitHub's merge queue handles branch deletion itself; gh rejects
          # --delete-branch when the target branch uses a merge queue.
          gh pr merge "$pr_url" --repo "$REPOSITORY" --auto --squash
    name: update
